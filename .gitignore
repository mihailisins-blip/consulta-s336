# deps
node_modules/

# generated data folders (the pipeline output)
/data/
data-v*/
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Flutter build output (Fase B/C)
app/build/
app/.dart_tool/
app/windows/flutter/ephemeral/

# heavy local fixtures (real corpus excerpts kept out of git)
pipeline/test/fixtures/real/

# ... except the app's own real *.sqlite test fixtures (see
# app/test/fixtures/README.md for provenance) -- *.sqlite above would
# otherwise also catch these legitimate, intentionally-committed ones.
!app/test/fixtures/data.sqlite
!app/test/fixtures/data-lotes.sqlite

# OS / editor
Thumbs.db
.DS_Store
.idea/
.vscode/
